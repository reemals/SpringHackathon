CREATE TABLE `conygre`.`shares` (
                                    `Symbol` VARCHAR(45) NOT NULL,
                                    `Price` INT NULL,
                                    PRIMARY KEY (`Symbol`));
ARY KEY (`Symbol`));